# Dependências mínimas para rodar sem banco de dados
# Suficiente para dados reais: NASA FIRMS + Open-Meteo + agente explicador

fastapi==0.115.5
uvicorn[standard]==0.32.1

# Pydantic e configuração
pydantic==2.10.3
pydantic-settings==2.6.1

# HTTP client
httpx==0.28.1

# LangChain + LangGraph (create_agent API requires langchain 1.x)
langchain>=1.3,<2
langchain-core>=1.4,<2
langchain-openai>=0.3,<2
langgraph>=1.2,<2

# RAG — índice FAISS do chat da pesquisa (fastembed: leve para EC2)
faiss-cpu>=1.8.0,<2
fastembed>=0.4.0,<0.7

# PyTorch (NeKo-PIGNN agent — CPU wheel on EC2)
torch>=2.5,<3

# Utilitários
python-dotenv==1.0.1
